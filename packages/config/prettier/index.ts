import type { Options } from "prettier";

const config: Options = {
  semi: true,
  singleQuote: true,
  trailingComma: "all",
  printWidth: 80,
  tabWidth: 2,
  bracketSpacing: true,
  arrowParens: "avoid",
  endOfLine: "lf",
};

export default config;
